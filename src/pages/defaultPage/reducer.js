let initialState = {

};

export default function defaultPageReducer(state, action) {
    switch (action.type) {
        case '':
            return {

            };
        default:
            return state;
    }
}